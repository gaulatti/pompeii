name: Deploy Backend

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
      - '.github/workflows/backend-deploy.yml'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Use Node 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: backend/package-lock.json

      - name: Install backend dependencies
        working-directory: backend
        run: npm ci

      - name: Test backend
        working-directory: backend
        run: npm test -- --runInBand

      - name: Build backend
        working-directory: backend
        run: npm run build

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.DEPLOYMENT_TOKEN }}

      - name: Build and push backend image
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          file: ./backend/Dockerfile
          pull: true
          push: true
          tags: ghcr.io/${{ github.repository }}-backend:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Deploy REST and gRPC service to host
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          script: |
            export AWS_CREDS="$HOME/.aws"
            sh -c '
            set -eu

            require_value() {
              value="$1"
              name="$2"
              if [ -z "$value" ]; then
                echo "Missing required GitHub secret or variable: $name"
                exit 1
              fi
            }

            require_value "${{ vars.HTTP_PORT }}" "HTTP_PORT"
            require_value "${{ vars.GRPC_PORT }}" "GRPC_PORT"
            require_value "${{ vars.AWS_REGION }}" "AWS_REGION"
            require_value "${{ vars.LOGS_GROUP }}" "LOGS_GROUP"
            require_value "${{ secrets.SECRET_ARN }}" "SECRET_ARN"
            require_value "${{ secrets.UNIQUE_KEY }}" "UNIQUE_KEY"

            docker login ghcr.io -u ${{ github.actor }} -p ${{ secrets.DEPLOYMENT_TOKEN }}
            docker pull ghcr.io/${{ github.repository }}-backend:latest

            docker stop pompeii-backend || true
            docker rm pompeii-backend || true

            LOG_STREAM_UUID=$(uuidgen)

            docker run -d --name pompeii-backend \
              -v "$AWS_CREDS:/root/.aws:ro" \
              -p ${{ vars.HTTP_PORT }}:3187 \
              -p ${{ vars.GRPC_PORT }}:50087 \
              -e NODE_ENV=production \
              -e AWS_REGION="${{ vars.AWS_REGION }}" \
              -e SECRET_ARN="${{ secrets.SECRET_ARN }}" \
              -e UNIQUE_KEY="${{ secrets.UNIQUE_KEY }}" \
              --restart=always \
              --log-driver=awslogs \
              --log-opt awslogs-region=${{ vars.AWS_REGION }} \
              --log-opt awslogs-group=${{ vars.LOGS_GROUP }} \
              --log-opt awslogs-stream="$LOG_STREAM_UUID" \
              ghcr.io/${{ github.repository }}-backend:latest

            for attempt in $(seq 1 45); do
              http_ready=false
              grpc_ready=false

              if docker exec pompeii-backend node -e "fetch(\"http://127.0.0.1:3187/authorization/health\").then((response) => process.exit(response.ok ? 0 : 1)).catch(() => process.exit(1))"; then
                http_ready=true
              fi

              if docker exec pompeii-backend node -e "const socket=require(\"node:net\").connect(50087,\"127.0.0.1\",()=>{socket.end();process.exit(0)});socket.setTimeout(1000,()=>{socket.destroy();process.exit(1)});socket.on(\"error\",()=>process.exit(1))"; then
                grpc_ready=true
              fi

              if [ "$http_ready" = true ] && [ "$grpc_ready" = true ]; then
                echo "Pompeii REST and gRPC health checks passed"
                exit 0
              fi

              sleep 2
            done

            echo "Pompeii failed to become healthy"
            docker logs --tail 200 pompeii-backend || true
            exit 1
            '
